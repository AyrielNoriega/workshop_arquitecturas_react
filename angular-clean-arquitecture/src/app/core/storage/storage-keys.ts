/** Claves de persistencia de sesión. Las usan los interceptores (core) y el repo de auth (data). */
export const ACCESS_TOKEN_KEY = 'auth.access_token';
export const REFRESH_TOKEN_KEY = 'auth.refresh_token';
